# LnisAfsCodec

This directory is a project-owned C ABI wrapper. The upstream source trees under
`오픈소스` are read-only inputs and are not modified by the build.

Build on Windows with `./build-wsl.ps1`. The output is written only to
`Native/LnisAfsCodec/bin/win-x64`.

