# LNIS AFS Validator 빠른 실행

1. 두 PC에 .NET 8 Desktop Runtime x64를 설치한다.
2. Windows 방화벽에서 UDP 데이터 포트 45821과 결과 포트 45822를 허용한다.
3. 수신 PC에서 `LnisAfsValidator.exe`를 실행하고 **수신부 → 수신 대기**를 누른다.
4. 송신 PC에서 **송신부**를 열고 `capture.graw`와 Broadcast 주소를 선택한다. 외부 알마낙 파일은 사용하지 않는다.
5. Test A는 Drop Rate 0%, Test E는 원하는 Drop Rate와 Seed를 입력하고 송신한다.
6. CRC·LDPC·인터리빙 성능은 시작 화면의 **오류정정 실험**에서 실행한다.

AFS 6000심볼은 750바이트로 패킹되어 한 UDP 데이터그램 payload로 전송된다. I/Q·RF 처리는 포함하지 않는다.

PRN 8은 현재 UDP 패킷의 논리 AFS 스트림 식별값이며 RF 위성 선택이나 PVT 계산에 사용되지 않는다.
