# LNIS AFS Validator 수동 인수시험

## 사전 기록

- 앱 버전/빌드 일시:
- `LnisAfsCodec.dll` SHA-256:
- 송신/수신 PC 주소:
- 데이터/결과 포트:
- 입력 `capture.graw` SHA-256:
- 논리 PRN 식별값(기본 8):

## 송신부 GNSS COM 입력

- 장치 관리자에 표시된 COM 포트와 Baud rate를 기록한다.
- 프로토콜 미정 모드에서 수집 후 `serial-input.bin` 크기가 증가하고 중지 시 파일이 정상 닫히는지 확인한다.
- 장비 프로토콜 어댑터가 제공되면 관측 Epoch·항법 Update 카운터와 `capture.graw` 생성을 확인한다.
- 생성된 `capture.graw`가 같은 송신부의 입력 경로에 자동 설정되는지 확인한다.
- 잘못된 COM 이름, 사용 중인 포트와 지원하지 않는 Baud rate 오류가 화면에 표시되는지 확인한다.

## Test A 정상 종단간

- 수신부를 먼저 시작한 뒤 송신부에서 `TestA_Normal`을 선택해 실행한다.
- 수신부에 Test A와 정상 전송 조건이 자동 표시되는지 확인한다.
- 수신 결과가 Pass이고 원본/복원 길이, 레코드 수, SHA-256이 모두 일치하는지 확인한다.
- `result.json`, `metrics-summary.csv`, `metrics-timeseries.csv`, `reconstructed.graw` 생성을 확인한다.

## Test B/C 종단 간 오류정정

- 수신부를 대기시키고 송신부에서 `TestB_RandomErrors`와 `TestC_BurstErrors`를 각각 선택한다.
- 오류 심볼 수와 Seed를 입력하고 실제 `capture.graw`를 송신한다.
- 수신부에 시험 종류와 Random/Burst 조건이 자동 표시되는지 확인한다.
- SB2/SB3/SB4 CRC 통과 프레임 수, LDPC 정정 심볼 수와 최종 RAW 무결성이 양쪽 결과에 기록되는지 확인한다.
- 같은 입력과 Seed로 재실행했을 때 판정과 정정 결과가 재현되는지 확인한다.

## Test D 재동기

- 송신부에서 `TestD_SyncRecovery`를 선택하고 SP 오류 개수 1~68, 손상 프레임 간격과 Seed를 입력한다.
- 입력 파일이 AFS 프레임 두 개 이상을 생성하는지 확인한다.
- 수신부에 Test D 조건이 자동 표시되는지 확인한다.
- 모든 논리 프레임 수신과 손상되지 않은 예상 프레임 수만큼 `RecoveredSyncFrames`·Decode가 기록되어 Pass인지 확인한다.
- 손상 프레임의 RAW 조각은 제외되므로 Test D의 전체 RAW 해시는 불일치할 수 있음을 확인한다.

## Test E UDP Drop

- 송신부에서 `TestE_UdpDrop`을 선택하고 Drop Rate 10%, 고정 Seed로 두 번 실행해 계획된 제거율이 같은지 확인한다.
- 수신부에 Test E와 Drop 조건이 자동 표시되는지 확인한다.
- SessionStart/End와 Result가 유지되고 시험이 정상 종료되는지 확인한다.

## 실패 경로

- 잘못된 파일, 같은 포트, 범위 밖 오류 개수·SP 오류 수·Drop Rate가 실행 전에 거부되는지 확인한다.
- 수신 대기와 송신 결과 대기 중 취소가 정상 동작하는지 확인한다.
- 방화벽 차단 또는 수신부 미실행 시 송신부에 타임아웃 오류가 표시되는지 확인한다.
